import { useCallback, useMemo } from 'react';
import { CRYPTREES_MINT_ABI } from '../config/abi';
import {
    OPENSEA_URL,
    NETWORK_NAME,
    CRYPTREES_ADDRESS,
    ALCHEMY_API_KEY,
    INFURA_API_KEY,
} from '../config'

const useMaxSupply = () => {

}